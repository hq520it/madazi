# HotSwapAgent 配置：项目为纯 MyBatis（无 mybatis-plus 依赖）
# 禁用值必须是 @Plugin 注解的 name（MyBatisPlus），不是类全名！
# 否则 agent 改写 XMLConfigBuilder 注入 MybatisConfiguration 导致 NoClassDefFoundError
disabledPlugins=MyBatisPlus
